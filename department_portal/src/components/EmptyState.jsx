const EmptyState = ({ message }) => (
  <div className="rounded-2xl border border-borderMain bg-cardBg p-5 text-sm text-textSecondary shadow-card">
    {message}
  </div>
);

export default EmptyState;
